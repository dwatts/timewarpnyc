export declare const clamp: (value: number, min: number, max: number) => number;
export declare const decimalPlaces: (value: number) => number;
export declare function remap(value: number, fromMin: number, fromMax: number, toMin: number, toMax: number): number;
/**
 * Helper to determine if a value is close to the edge of a range within a threshold.
 *
 * @param value
 * @param range
 * @param threshold
 * @returns -1 if close to lower edge, 1 if close to upper edge, 0 otherwise.
 */
export declare function closeToRangeEdge(value: number, range: number, threshold: number): number;
